p-stop:always;
  }
  body::before{left:8px;opacity:.4}
  body::after{left:5px}
}

@media (prefers-reduced-motion:no-preference){

  /* HERO — cinematic entrance without recoloring */
  .hero-intro,.hero-copy,.hero-visual,.hero-logo-wrap{
    will-change:transform,opacity,filter;
  }
  .hero-logo-wrap{
    opacity:0;
    transform:translateY(-24px) scale(.9);
    animation:hrLogoDrop 1s cubic-bezier(.16,1,.3,1) .12s forwards;
  }
  .hero-copy h1 > *{
    display:inline-block;
    opacity:0;
    transform:translateY(56px) rotateX(-22deg);
    transform-origin:50% 100%;
    animation:hrHeadlineIn 1.05s cubic-bezier(.16,1,.3,1) forwards;
  }
  .hero-copy h1 > *:nth-child(1){animation-delay:.22s}
  .hero-copy h1 > *:nth-child(2){animation-delay:.38s}
  .hero-copy h1 > *:nth-child(3){animation-delay:.54s}
  .hero-buttons>*{
    opacity:0;
    transform:translateY(24px) scale(.94);
    animation:hrBtnIn .8s cubic-bezier(.16,1,.3,1) .82s forwards;
  }
  .hero-buttons>*:nth-child(2){animation-delay:.94s}
  .hero-visual{
    opacity:0;
    transform:translateY(36px) scale(.94);
    filter:blur(8px);
    animation:hrHeroVisualIn 1.25s cubic-bezier(.16,1,.3,1) .3s forwards;
  }
  .hero-visual img{
    will-change:transform;
    animation:hrHeroFloat 7.5s ease-in-out 1.5s infinite alternate;
  }

  @keyframes hrLogoDrop{to{opacity:1;transform:none}}
  @keyframes hrHeadlineIn{to{opacity:1;transform:none}}
  @keyframes hrBtnIn{to{opacity:1;transform:none}}
  @keyframes hrHeroVisualIn{to{opacity:1;transform:none;filter:none}}
  @keyframes hrHeroFloat{
    from{transform:translate3d(0,0,0) scale(1.01)}
    to{transform:translate3d(0,-10px,0) scale(1.045)}
  }

  /* Story reveals — alternating directions create flow, not stacked blocks */
  .hr-story{
    opacity:0;
    filter:blur(7px);
    transform:translate3d(0,46px,0) scale(.975);
    transition:
      opacity .95s cubic-bezier(.16,1,.3,1),
      transform .95s cubic-bezier(.16,1,.3,1),
      filter .95s ease;
    will-change:transform,opacity,filter;
  }
  .hr-story.hr-left{transform:translate3d(-42px,28px,0) scale(.98)}
  .hr-story.hr-right{transform:translate3d(42px,28px,0) scale(.98)}
  .hr-story.hr-in{
    opacity:1;
    filter:none;
    transform:none;
  }

  /* Section titles feel like chapter openings */
  .section-title.hr-story{
    transform:translateY(34px) scale(.94);
  }
  .section-title.hr-in{
    transform:none;
  }

  /* Service cards: depth/scene focus, while retaining original visual design */
  .service-card{
    transform-style:preserve-3d;
    will-change:transform;
    transition:
      transform .62s cubic-bezier(.16,1,.3,1),
      box-shadow .62s ease!important;
  }
  .service-card img{
    will-change:transform,filter;
    transition:
      transform .9s cubic-bezier(.16,1,.3,1),
      filter .6s ease!important;
  }
  .service-card.hr-focus{
    transform:translateY(-10px) scale(1.018) rotateX(1.2deg)!important;
  }
  .service-card.hr-focus img{
    transform:scale(1.055);
  }

  /* Make chapters breathe together */
  .feature-chip,.step-card,.timeline-item,.join-content,.faq details,.cta-box,.social-grid a{
    transition:
      transform .55s cubic-bezier(.16,1,.3,1),
      box-shadow .55s ease!important;
  }
  .feature-chip:hover,.social-grid a:hover,.step-card:hover{
    transform:translateY(-7px) scale(1.018);
  }

  /* FAQ feels like unfolding story */
  .faq details[open]{
    animation:hrFaq .48s cubic-bezier(.16,1,.3,1);
  }
  @keyframes hrFaq{
    from{opacity:.72;transform:scale(.985)}
    to{opacity:1;transform:none}
  }

  /* Slow parallax through major scenic areas */
  .hero,.join,.final-cta,.social{
    background-position:center calc(50% + var(--hr-parallax,0px))!important;
  }

  /* Gentle pulse on decorative accents without altering their colors */
  .hero-heart,.neon-word{
    animation:hrAccentPulse 2.8s ease-in-out infinite;
  }
  @keyframes hrAccentPulse{
    0%,100%{filter:brightness(1);transform:translateZ(0)}
    50%{filter:brightness(1.12);transform:translateY(-2px)}
  }
}

/* Respect accessibility */
@media (prefers-reduced-motion:reduce){
  *,*::before,*::after{
    animation-duration:.001ms!important;
    animation-iteration-count:1!important;
    scroll-behavior:auto!important;
    transition-duration:.001ms!important;
  }
  body::after{display:none}
}
</style>

</head>
<body>
  <div class="preloader" id="preloader" aria-label="HerRide loading">
    <img src="https://herride.pages.dev/assets/logo.png" alt="HerRide logo" />
    <span data-i18n="loading">جاري تجهيز رحلتك...</span>
  </div>

  <header class="site-header">
    <a class="brand" href="#home" aria-label="HerRide Home">
      <img src="https://herride.pages.dev/assets/logo.png" alt="HerRide logo" />
    </a>
    <nav class="nav" id="nav">
      <a href="#home